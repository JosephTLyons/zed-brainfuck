[
  (memoryadd)      ; +
  (memorysubtract) ; -
  (memoryinput)    ; ,
  (memoryoutput)   ; .
] @operator

[
  (memoryinput)    ; ,
  (memoryoutput)   ; .
] @punctuation.delimiter

[
  (pointerleft)  ; <
  (pointerright) ; >
] @storageclass

(comment) @comment

;; skipping brackets []
